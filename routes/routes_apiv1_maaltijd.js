const express = require ("express");
const router = express.Router;
const maaltijd_manager = require("../managers/maaltijd_manager");

// hier schrijven we router endpoints
// router.get('/api/studentenhuis/:huisId/maaltijd', maaltijd_manager.getMaaltijd);
// router.post('/', maaltijd_controller.createMaaltijd);
// router.get('/api/studentenhuis/:huisId/maaltijd/:maaltijdId', maaltijd_manager.getMaaltijdById);
// router.put('/api/studentenhuis/:huisId/maaltijd/:maaltijdId', maaltijd_manager.putMaaltijd);
// router.delete('/api/studentenhuis/:huisId/maaltijd/:maaltijdId', maaltijd_manager.deleteMaaltijd);

module.exports = router;

